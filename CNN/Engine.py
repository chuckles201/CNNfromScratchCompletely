'''This file will have the custom-class that we will backpropogate through eventually, and use to compute the transformations caused by layers. Similar to a 
a PyTorch Tensor, but much simpler!

The operations we need:

'''


# Parent class to store our data and preform transformations on it. (doesn't support broadcasting)

class AwesomeHolder: 
  def __init__(values):
    self.values = values
    
  def matrixmul():

  def add():

  def sub():

  def divide():

  def multiply():

